# PrismForge UI 架构示意图

## 📊 页面导航结构图

```
┌─────────────────────────────────────────────────────────────────┐
│                        PrismForge 应用                          │
│                      Tauri 2 + React 18                         │
└─────────────────────────────────────────────────────────────────┘
                                  │
                ┌─────────────────┼─────────────────┐
                │                 │                 │
           ┌────▼────┐     ┌─────▼──────┐    ┌────▼─────┐
           │  主页    │     │ 会话管理    │    │  设置页面  │
           │   /     │     │  /sessions  │    │ /settings │
           └────┬────┘     └─────┬──────┘    └────┬─────┘
                │                 │                 │
                │                 │                 │
           ┌────▼────┐     ┌─────▼──────┐    ┌────▼─────┐
           │会话监控  │────▶│ 会话详情    │    │ 提供商    │
           │提示词优化 │     │:sessionId   │    │ 向量搜索  │
           └─────────┘     └────────────┘    └──────────┘
                                                     │
                                                     │
                                              ┌─────▼──────┐
                                              │设置页面v2   │
                                              │/settings/v2│
                                              └────────────┘
                                                     │
                                              ┌─────▼──────┐
                                              │提示词实验室 │
                                              │/prompt-lab │
                                              └────────────┘
```

## 🏗️ 详细页面布局结构

### 1. 主页 (`/` - App.tsx)

```
┌─────────────────────────────────────────────────────────────┐
│  Claude Session Monitor                                      │
│  ┌─────────────┐  ┌──────────────┐  ┌─────────────────┐   │
│  │ 📂会话管理   │  │ ⚙️ 设置(F6)  │  │ 🌓 主题切换      │   │
│  └─────────────┘  └──────────────┘  └─────────────────┘   │
├─────────────────────────────────────────────────────────────┤
│  配置区域                                                     │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ Session File: [________________] [Auto Detect]       │  │
│  │ Next Goal:    [________________]                    │  │
│  │              [Analyze & Generate Prompt]             │  │
│  └──────────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────────┤
│  左侧: AI 建议结果            │   右侧: Session Log (调试)   │
│  ┌────────────────────────┐ │   ┌────────────────────────┐ │
│  │ AI Analysis Result     │ │   │ Session Log (n)         │ │
│  │ ┌────────────────────┐ │ │   │ [Refresh] [▶自动刷新]  │ │
│  │ │ Result will appear │ │ │   ├────────────────────────┤ │
│  │ │ here...            │ │ │   │ 🔵 USER 12:34:56       │ │
│  │ │                    │ │ │   │ Content preview...     │ │
│  │ └────────────────────┘ │ │   │ 🟢 ASSIST 12:35:10     │ │
│  └────────────────────────┘ │   │ Content preview...     │ │
│                             │   └────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

**功能特性：**
- 📁 会话文件自动检测和加载
- ✨ AI 分析和提示词生成
- 🔄 自动刷新机制（3秒间隔）
- 📋 实时日志解析预览

### 2. 会话管理页 (`/sessions` - SessionsPage.tsx)

```
┌─────────────────────────────────────────────────────────────┐
│  ← 返回  │  会话管理    │  浏览和管理 Claude Code 会话历史 │ 🌓│
├─────────────────────────────────────────────────────────────┤
│  左侧: 项目侧边栏 (356px)  │  主内容区域                      │
│  ┌────────────────────┐   │  ┌────────────────────────────┐│
│  │ ProjectSidebar     │   │  │ SessionFileList            ││
│  │ ┌────────────────┐ │   │  │ ┌────────────────────────┐ ││
│  │ │ 📁 项目A       │ │   │  │ │ 📄 session-001.jsonl  │ ││
│  │ │ 📁 项目B       │ │   │  │ │ 📄 session-002.jsonl  │ ││
│  │ │ 📁 项目C       │ │   │  │ │ 📄 session-003.jsonl  │ ││
│  │ └────────────────┘ │   │  │ └────────────────────────┘ ││
│  └────────────────────┘   │  └────────────────────────────┘│
│                            │                                   │
│                            │  (或 SessionContentView 视图)    │
│                            │  ┌────────────────────────────┐│
│                            │  │ 消息树 / 差异对比 / 导出   ││
│                            │  └────────────────────────────┘│
└─────────────────────────────────────────────────────────────┘
```

**功能特性：**
- 🗂️ 项目目录浏览和筛选
- 📄 会话文件列表展示
- 👁️ 双视图模式：列表视图 / 内容视图
- 🔄 返回主页导航

**视图状态：**
- `directory_list`: 显示选中目录的会话文件列表
- `session_content`: 显示选中会话的详细内容

### 3. 会话详情页 (`/sessions/:sessionId` - SessionDetailPage.tsx)

```
┌─────────────────────────────────────────────────────────────┐
│  ← 返回  │  会话详情              │  [标签] [评分] [导出]  │ 🌓│
├─────────────────────────────────────────────────────────────┤
│  ┌────────────────────────────────────────────────────────┐ │
│  │ Token 统计卡片                                          │ │
│  │ 输入: 12,345  输出: 6,789  总计: 19,134  消息数: 42    │ │
│  └────────────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────────────┤
│  视图等级选择器                                              │
│  ◉ L2 Clean Flow  ○ L3 Detailed  ○ Full Context            │
├─────────────────────────────────────────────────────────────┤
│  消息树 (MessageTree)                                        │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ 📂 Root Thread                                         │ │
│  │   ├─ 👤 User: 初始问题...                              │ │
│  │   │   └─ 🤖 Assistant: 回答...                         │ │
│  │   │       ├─ 👤 User: 追问...                          │ │
│  │   │       └─ 🤖 Assistant: 详细解释...                 │ │
│  │   └─ 🔄 Branch: 替代方案                              │ │
│  └────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

**功能特性：**
- 📊 Token 统计（输入/输出/总计）
- 🎯 视图等级选择器（L2/L3/Full）
- 🌲 消息树展示（支持分支）
- ⭐ 会话评分（1-5星）
- 🏷️ 标签编辑和管理
- 📤 多格式导出（JSON/Markdown/Text）

**URL 参数：**
- `view`: 视图等级（`l2_clean_flow` | `l3_detailed` | `full_context`）
- `sessionId`: 会话唯一标识

### 4. 设置页面 v2 (`/settings/v2` - SettingsPage.tsx)

```
┌─────────────────────────────────────────────────────────────┐
│  ← 返回  │  设置  │  [+ 新建提供商]  │  🌓 主题切换        │
├─────────────────────────────────────────────────────────────┤
│  [提供商设置]  [向量搜索设置]                                │
├─────────────────────────────────────────────────────────────┤
│  提供商列表                                                  │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ Provider ┃ Base URL        ┃ Model ┃ Status ┃ Actions│ │
│  │ ─────────╇─────────────────╇───────╇───────╇───────│ │
│  │ OpenAI   ┃ api.openai.com  ┃ GPT-4 ┃ ✅     │ ✏️ 🗑️│ │
│  │ Anthropic┃ api.anthropic...┃ Claude┃ ✅     │ ✏️ 🗑️│ │
│  │ Ollama   ┃ localhost:11434 ┃ llama3┃ ⚠️     │ ✏️ 🗑️│ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
│  或 向量搜索设置                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ 向量搜索配置                                             │ │
│  │ [启用向量搜索]                                          │ │
│  │ Embedding 模型: [text-embedding-3-small      ]         │ │
│  │ 维度数量:      [1536                        ]          │ │
│  │ [测试连接]  [保存配置]                                   │ │
│  └────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

**功能特性：**
- 🔑 API 提供商管理（CRUD）
- 🔌 连接测试和状态监控
- 🔍 向量搜索配置
- 💾 安全密钥存储（Keyring）

**标签页：**
- `providers`: 提供商设置
- `vector`: 向量搜索设置

**视图模式：**
- `list`: 提供商列表
- `create`: 新建提供商对话框
- `edit`: 编辑提供商对话框

### 5. 提示词实验室 (`/prompt-lab` - PromptLab.tsx)

```
┌─────────────────────────────────────────────────────────────┐
│  🪄 提示词实验室                                             │
│  基于 AI 和历史会话生成优化的提示词，提高开发效率             │  🌓│
├─────────────────────────────────────────────────────────────┤
│  [生成器]  [提示词库]                                        │
├─────────────────────────────────────────────────────────────┤
│  左侧: 提示词构建器 (2/3)    │  右侧: 统计信息 (1/3)        │
│  ┌────────────────────────┐ │  ┌────────────────────────┐ │
│  │ 会话文件: [________]   │ │  │ 📊 生成统计            │ │
│  │ [选择文件]             │ │  │ 总生成: 128            │ │
│  │                        │ │  │ 今日: 12               │ │
│  │ 目标描述:              │ │  │                        │ │
│  │ [______________]       │ │  │ 📈 质量分布            │ │
│  │ [______________]       │ │  │ ⭐⭐⭐⭐⭐ 45%         │ │
│  │                        │ │  │ ⭐⭐⭐⭐   30%         │ │
│  │ [选择会话模板]         │ │  │                        │ │
│  │ [选择优化策略]         │ │  │ 🎯 快速操作            │ │
│  │                        │ │  │ [导入] [导出]         │ │
│  │ [✨ 生成优化提示词]     │ │  │                        │ │
│  └────────────────────────┘ │  └────────────────────────┘ │
│                                                              │
│  最近生成                                                    │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ 🎯 "修复用户认证问题"              ⭐⭐⭐⭐⭐ [复制]    │ │
│  │ 生成时间: 2025-01-13 14:23                              │ │
│  └────────────────────────────────────────────────────────┘ │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ 🎯 "优化数据库查询性能"            ⭐⭐⭐⭐   [复制]    │ │
│  │ 生成时间: 2025-01-13 13:15                              │ │
│  └────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

**功能特性：**
- 🪄 AI 提示词生成
- 📚 提示词库管理
- 📊 生成统计和质量分析
- 🎯 会话模板和优化策略
- 💾 导入/导出功能

**标签页：**
- `builder`: 提示词生成器
- `library`: 提示词库

## 🔄 页面导航关系

### 用户操作流程

```
1. 启动应用
   └─► 主页 (/)
       ├─► 点击 "📂 会话管理" ──► /sessions
       ├─► 点击 "⚙️ 设置 (F6)" ──► /settings/v2
       └─► F6 快捷键 ──► /settings

2. 会话管理
   └─► /sessions
       ├─► 选择项目 ──► 显示会话列表
       ├─► 点击会话 ──► /sessions/:sessionId
       └─► ← 返回按钮 ──► /

3. 会话详情
   └─► /sessions/:sessionId
       ├─► 查看消息树
       ├─► 导出会话
       ├─► 评分/标签
       └─► ← 返回按钮 ──► /sessions

4. 设置页面
   └─► /settings/v2
       ├─► 提供商管理
       ├─► 向量搜索配置
       └─► ← 返回按钮 ──► /

5. 提示词实验室
   └─► /prompt-lab
       ├─► 生成器 ──► 生成提示词
       ├─► 提示词库 ──► 查看历史
       └─► 返回主页 ──► /
```

### 编程式导航

```typescript
// React Router 导航示例
import { useNavigate } from 'react-router-dom';

const navigate = useNavigate();

// 导航到各个页面
navigate('/');                    // 主页
navigate('/sessions');            // 会话管理
navigate('/sessions/abc123');     // 会话详情
navigate('/settings');            // 旧版设置
navigate('/settings/v2');         // 新版设置
navigate('/prompt-lab');          // 提示词实验室
```

## 📦 核心组件依赖关系

```
┌─────────────────────────────────────────────────────────────┐
│                      共享组件层                              │
│  ThemeToggle | Button | Card | Dialog | Tabs | Alert        │
└─────────────────────────────────────────────────────────────┘
          │                    │                    │
          ▼                    ▼                    ▼
┌───────────────┐    ┌──────────────┐    ┌──────────────┐
│ 业务组件层     │    │ 业务组件层    │    │ 业务组件层    │
├───────────────┤    ├──────────────┤    ├──────────────┤
│ ProjectSidebar │    │ MessageTree   │    │ PromptBuilder│
│ SessionFileList│    │ TokenCounter  │    │ PromptLibrary│
│ SessionContentView│ SessionRating │    │ ProviderForm │
│ ProviderSettings│ TagEditor      │    │ VectorSettings│
└───────────────┘    └──────────────┘    └──────────────┘
          │                    │                    │
          └────────────────────┼────────────────────┘
                               ▼
                    ┌──────────────────┐
                    │   Zustand Store  │
                    │ ──────────────── │
                    │ useSettingsStore │
                    │ useSessionStore  │
                    └──────────────────┘
```

## 🎨 UI 技术栈

### 核心框架
- **框架**: React 18.3 + TypeScript
- **路由**: React Router DOM 6.30
- **状态管理**: Zustand 5.0 + Immer
- **构建工具**: Vite 7.0
- **桌面框架**: Tauri 2.0

### UI 组件库
- **基础组件**: Shadcn UI (Radix UI + Tailwind CSS)
- **样式方案**: Tailwind CSS 3.x
- **图标库**: Lucide React
- **主题系统**: 亮色/暗色主题切换 (ThemeContext)
- **代码编辑器**: Monaco Editor

### 数据管理
- **状态管理**: Zustand (全局状态)
- **数据缓存**: TanStack Query (React Query)
- **表单管理**: React Hook Form 7.69

### 工具库
- **日期处理**: date-fns
- **路径处理**: Tauri API (path)
- **文件系统**: Tauri API (fs)

## 🔑 关键导航入口

### 主页导航栏
- **📂 会话管理**: 导航到 `/sessions`
- **⚙️ 设置 (F6)**: 导航到 `/settings`

### 快捷键
- **F6**: 快速跳转到设置页面

### 编程式导航
```typescript
// 主导航入口
navigate('/')                 // 主页 - Claude Session Monitor
navigate('/sessions')         // 会话管理页面
navigate('/sessions/:id')     // 会话详情页面（动态路由）
navigate('/settings')         // 旧版设置页面
navigate('/settings/v2')      // 新版设置页面（Shadcn UI）
navigate('/prompt-lab')       // 提示词实验室
```

## 📁 文件结构

```
src/
├── main.tsx                    # 路由配置和懒加载
├── App.tsx                     # 主页组件
├── pages/                      # 页面级组件
│   ├── App.tsx                 # 主页（会话监控）
│   ├── Settings.tsx            # 旧版设置页面
│   ├── SettingsPage.tsx        # 新版设置页面
│   ├── SessionsPage.tsx        # 会话管理页面
│   ├── SessionDetailPage.tsx   # 会话详情页面
│   └── PromptLab.tsx           # 提示词实验室
├── components/                 # 可复用组件
│   ├── ui/                     # Shadcn UI 基础组件
│   ├── settings/               # 设置相关组件
│   ├── ProjectSidebar.tsx      # 项目侧边栏
│   ├── SessionFileList.tsx     # 会话文件列表
│   ├── SessionContentView.tsx  # 会话内容视图
│   ├── MessageTree.tsx         # 消息树组件
│   ├── TokenCounter.tsx        # Token 统计组件
│   ├── PromptBuilder.tsx       # 提示词构建器
│   ├── PromptLibrary.tsx       # 提示词库
│   └── ThemeToggle.tsx         # 主题切换
├── stores/                     # Zustand 状态管理
│   ├── useSettingsStore.ts     # 设置状态
│   └── useSessionStore.ts      # 会话状态
└── contexts/                   # React Context
    └── ThemeContext.tsx        # 主题上下文
```

## 🎯 页面特性总结

| 页面 | 路由 | 主要功能 | 关键特性 |
|------|------|----------|----------|
| 主页 | `/` | 会话监控和提示词生成 | 自动检测、AI 分析、实时刷新 |
| 会话管理 | `/sessions` | 项目和会话浏览 | 双视图、侧边栏、文件列表 |
| 会话详情 | `/sessions/:id` | 会话内容查看和分析 | 消息树、Token 统计、评分导出 |
| 设置 v1 | `/settings` | 提供商管理（旧版） | 基础 CRUD |
| 设置 v2 | `/settings/v2` | 提供商和向量配置 | Shadcn UI、双标签页 |
| 提示词实验室 | `/prompt-lab` | 提示词生成和管理 | AI 生成、统计、库管理 |

## 🚀 路由配置（main.tsx）

```typescript
// 代码分割：路由级懒加载
const Settings = lazy(() => import("./pages/Settings"));
const SettingsPage = lazy(() => import("./pages/SettingsPage"));
const SessionsPage = lazy(() => import("./pages/SessionsPage"));
const SessionDetailPage = lazy(() => import("./pages/SessionDetailPage"));
const PromptLab = lazy(() => import("./pages/PromptLab"));

// 路由配置
<Routes>
  <Route path="/" element={<App />} />
  <Route path="/settings" element={<Settings />} />
  <Route path="/settings/v2" element={<SettingsPage />} />
  <Route path="/sessions" element={<SessionsPage />} />
  <Route path="/sessions/:sessionId" element={<SessionDetailPage />} />
  <Route path="/prompt-lab" element={<PromptLab />} />
</Routes>
```

---

**生成时间**: 2025-01-13
**项目**: PrismForge
**技术栈**: Tauri 2 + React 18 + TypeScript + Shadcn UI
