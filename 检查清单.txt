# UI 更新检查清单

## ✅ 已完成的工作

1. ✅ 创建了 MessageCard 组件
2. ✅ 创建了 SessionStatsSidebar 组件
3. ✅ 创建了 SessionDetailPageV2 页面
4. ✅ 更新了路由配置（使用 V2）
5. ✅ 前端构建成功（14.36 kB）

## 🔍 为什么看不到新 UI？

### 可能原因 1: Tauri WebView 缓存
**解决方法：**
```bash
# 1. 完全停止应用
# 关闭所有 prism-forge.exe 窗口

# 2. 清除所有缓存
rm -rf node_modules/.vite
rm -rf dist
rm -rf src-tauri/target/debug

# 3. 重新构建
npm run build

# 4. 启动开发模式
npm run tauri dev
```

### 可能原因 2: 查看的是错误的页面
**确认：**
- 您访问的是 `/sessions/<sessionId>` 页面
- 不是 `/sessions` 列表页
- 不是 `/settings` 设置页

### 可能原因 3: 开发者工具未正确打开
**Tauri 应用快捷键：**
- `Ctrl + Shift + I`（不是 F12）
- 或者右键点击应用窗口 → "检查元素"

### 可能原因 4: 浏览器缓存
**在开发者工具中：**
1. 打开 Network 标签
2. 勾选 "Disable cache"
3. 刷新页面（Ctrl + R）

## 🎯 验证新 UI 的 3 种方法

### 方法 1: 查看红色标记（最明显）
位置：页面右上角
```
┌─────────────────┐
│  ✅ V2 NEW UI  │  ← 红色背景，白色文字
└─────────────────┘
```

### 方法 2: 查看控制台日志
1. 按 `Ctrl + Shift + I` 打开开发者工具
2. 点击 "Console" 标签
3. 查找以下日志：
```javascript
🚀 [SessionDetailPageV2] 组件已加载！！！新 UI 应该显示
📍 当前 URL: /sessions/xxx
```

**如果看到的是：**
```javascript
[SessionDetailPage] ...
```
**说明：** 加载的是旧版本！

### 方法 3: 查看 Network 标签
1. 按 `Ctrl + Shift + I` 打开开发者工具
2. 点击 "Network" 标签
3. 刷新页面
4. 查找以下文件：
```
SessionDetailPageV2-BMMKXVti.js    ← 应该存在
```

## 🚨 如果以上方法都不行

### 最后手段：完全重建

```bash
# 1. 停止所有进程
# 关闭应用窗口和所有终端

# 2. 删除所有构建文件
rm -rf node_modules/.vite
rm -rf dist
rm -rf src-tauri/target

# 3. 重新安装依赖（可选）
npm install

# 4. 重新构建
npm run build

# 5. 启动开发模式
npm run tauri dev
```

## 📞 反馈信息

如果仍然看不到新 UI，请提供：

1. **右上角是否有红色标记 "✅ V2 NEW UI"？**
   - [ ] 有
   - [ ] 没有

2. **开发者工具（Ctrl+Shift+I）Console 显示什么？**
   - [ ] 🚀 [SessionDetailPageV2] 组件已加载
   - [ ] [SessionDetailPage] （旧版本）
   - [ ] 其他：___________

3. **Network 标签是否加载了 SessionDetailPageV2-xxx.js？**
   - [ ] 是
   - [ ] 否

4. **您访问的页面路径是什么？**
   - 例如：`/sessions/abc123def456`
